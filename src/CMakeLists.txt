# Intentionally empty for Phase 0.
#
# Module libraries (domain, ledger, formula, cli) are added here one at a
# time, each in its own phase, via add_subdirectory(). No module folders
# are pre-created — see Phase 0 architecture notes.
